describe("arquivo testado", () => {
    it("/calculator", () => {
        expect(3).toEqual(3);
    });
  
    it("returns false for invalid params", () => {
      // Aqui vai o código desse teste
    });
  });